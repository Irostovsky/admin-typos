# Typus Models Configuration File
#
# Use the README file as a reference to customize settings.

Club:
  fields:
    default: name, exclusive, created_at
    form: name, exclusive
  order_by: 
  relationships: 
  filters: created_at, updated_at
  search: name
  application: admin-typos

